# gcs-viewer-private-files

1. install library before run
2. add your service account to project folder
3. fill the .env file, example : 

```
GOOGLE_APPLICATION_CREDENTIALS=your-location-gcp-iam-service-account
BUCKET_NAME=your-bucket-name
FILE_NAME=your-location-and-file-name
```